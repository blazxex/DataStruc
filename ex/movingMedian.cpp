#include<iostream>
#include<vector>

using namespace std;

int main(){

    std::ios_base::sync_with_stdio(false); std::cin.tie(0);
    int n,w,temp;
    cin>>n>>w;
    vector<int> num;
    while(n--){
        cin>>temp;
        num.push_back(temp);
    }
    sort(num.begin(),num.end());
    int a = 0

    int it = 1 +((b-a)/2)


}